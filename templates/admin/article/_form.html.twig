{{ form_start(form) }}
    <div class="row">
        <div class="col-3">{{ form_row(form.title) }}</div>
    </div>
    <div class="row">
        <div class="col-3">{{ form_row(form.content) }}</div>
    </div>
    <div class="row">
        <div class="col-3">{{ form_row(form.image) }}</div>
    </div> 
    <div class="row">
        <div class="col-3">{{ form_row(form.category) }}</div>
    </div>    
    {{ form_widget(form) }}
    <button type="submit"class="btn btn-default">{{ button|default('Enregistrer')}}</button>
{{ form_end(form) }}